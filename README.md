# python-simple-parser

python-simple-parser it's a simple parser for ozon site.
It receives a certain number of cards and displays their name, price, and link.

# Quick start

You can just run the file with no changes or you can change the amount
of iterations like this:
  ```
  getProducts(page, iterations=your_number)
  ```
